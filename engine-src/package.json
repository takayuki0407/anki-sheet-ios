{
  "name": "anki-sheet-engine",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "WebView PDF render + red-sheet color-detection engine (ported from anki-sheet web app).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "copy-assets": "node scripts/copy-pdfjs-assets.mjs",
    "prebuild": "node scripts/copy-pdfjs-assets.mjs",
    "build": "vite build"
  },
  "dependencies": {
    "pdfjs-dist": "6.0.227"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.0",
    "@types/node": "^25.9.1",
    "typescript": "~6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
