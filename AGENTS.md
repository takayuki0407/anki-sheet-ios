# Expo

プロジェクトは **Expo SDK 54（React Native 0.81）**。Expo は API が大きく変わっているため、
コードを書く前に必ず稼働中バージョンの正式ドキュメント https://docs.expo.dev/versions/v54.0.0/ を参照すること。

（SDK 55/56 へ上げる予定は `docs/tasks.md` / `../HANDOFF.md` 側に記載する。現行の参照ドキュメントは常に稼働中バージョン＝v54 に合わせる。）
